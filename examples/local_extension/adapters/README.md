# Local Adapter Layer

This folder is a local adapter envelope for the `local_extension` example.

## What it is
- A protocol shell for local adapter experiments
- A place to keep the adapter boundary explicit
- A sample structure for future OS / multi-agent / external project adapters

## What it is not
- Not a second core
- Not a runtime platform
- Not a replacement for `harness_core`
